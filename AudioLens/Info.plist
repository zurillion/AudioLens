<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Partial Info.plist. GENERATE_INFOPLIST_FILE is still on, so the standard
	  keys (CFBundleIdentifier, principal class, versions, etc.) are merged on
	  top of this file at build time. We only declare what the generator can't:
	  the audio document types, so the Dock icon and Finder "Open With" accept
	  audio files (which then route through application(_:open:)).
	-->
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Audio File</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>public.audio</string>
				<string>public.mp3</string>
				<string>com.microsoft.waveform-audio</string>
				<string>public.aiff-audio</string>
				<string>public.aifc-audio</string>
				<string>com.apple.m4a-audio</string>
				<string>public.mpeg-4-audio</string>
				<string>org.xiph.flac</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
